# Path to your oh-my-zsh installation.
export ZSH=$HOME/.oh-my-zsh

ZSH_THEME={{ ws.zsh.theme | default('random') }}

#The ÄNPL! All nodes, please land!
alias anpl='ansible-playbook -i hosts site.yml'
# for using git from source
source {{ ws.ansible.path }}/hacking/env-setup -q

plugins=({{ ws.zsh.plugins | default('git') }})

# User configuration

export PATH=$HOME/bin:/usr/local/bin:$PATH
# export MANPATH="/usr/local/man:$MANPATH"

source $ZSH/oh-my-zsh.sh

